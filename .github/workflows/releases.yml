name: Release

on:
  push:
    branches:
      - release

jobs:
  python-package:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      # Setups
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12' 

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install python packages
        working-directory: py-interface
        run: pip install -r requirements.txt

      # Build
      - name: Build Cli
        working-directory: py-interface
        run: cargo build --workspace --release

      - name: Build Python Interface (devmode)
        working-directory: py-interface
        run: maturin develop

      - name: Generate types
        working-directory: py-interface
        run: cargo run --bin stub_gen

      - name: Build Python Interface
        working-directory: py-interface
        run: maturin build

      - name: Upload Wheels
        uses: actions/upload-artifact@v4
        with:
          name: Python package
          retention-days: 5
          path: |
            target/wheels/*.whl

  build-msi:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      # Setups
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Npm Install
        working-directory: gui
        run: npm i

      - name: Build Cli
        working-directory: cli
        run: cargo build --release

      - name: Build Gui
        working-directory: gui
        run: npm run tauri build -- --release

      # Uploads
      - name: Upload Msi
        uses: actions/upload-artifact@v4
        with:
          name: DataSets-Manager-MSI
          retention-days: 5
          path: |
            target/release/bundle/msi/*.msi

      - name: Upload Exe
        uses: actions/upload-artifact@v4
        with:
          name: DataSets-Manager-EXE
          retention-days: 5
          path: |
            target/release/bundle/nsis/*.exe