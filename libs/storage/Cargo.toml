[package]
edition = "2024"
name = "storage"
version = "0.1.0"

[dependencies]
# Internal
interfaces.workspace = true
serializer.workspace = true

# External
anyhow.workspace = true
bhomz = {workspace = true, features = ["anyhow"]}

log.workspace = true

serde.workspace = true
serde_json = {workspace = true, features = ["float_roundtrip"] }
